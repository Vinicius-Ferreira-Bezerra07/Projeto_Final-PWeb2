export function CardStores({deals, stores}){

    console.log(stores)
    // console.log(deals);

    return(
        <>
            {stores.map((x) => (
                <h1>t</h1>
            ))}
        </>
    )
}